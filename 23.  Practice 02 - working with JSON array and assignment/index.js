console.log('script loaded');
const student = {
    "name":"John",
    "age":20
}
const students = [student];
const sumofagees = students.reduce((sum, current)=> sum+=current.age, 0)
const avg = s
console.log('done')
